# Absolute URLs mode — no dev proxy. Backend CORS (RNF10) already allows
# http://localhost:5173, so requests hit these hosts directly.
VITE_REPERTOIRE_URL=http://localhost:5001
VITE_PRACTICE_URL=http://localhost:5002
